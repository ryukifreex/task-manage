// import useAxios from 'axios-hooks'
// import { getTasks } from '../services/tasks'

// export const useTasks = () => {
//   const [{ data, loading, error }, refetch] = useAxios(getTasks, { manual: true })
//   return { data, loading, error, refetch }
// }
